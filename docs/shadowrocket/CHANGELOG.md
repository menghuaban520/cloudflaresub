# Changelog

## 5.1.0-rc.1 — 2026-09-08

- 新增独立配置，内嵌仓库自有覆盖表和国内核心表，消除规则文件下载依赖。
- 提供关闭广告拦截的对照配置，DNS / 隧道参数保持一致。
- 新增离线构建器、输入输出 SHA-256 清单、自动检查和候选 artifact。
- 补齐安装、回退、排障和实机验收记录。
- 保留 v5 主配置 URL 和手动选节点行为。外部大规则表不包含在独立版中。
- 已知限制：尚未实机验收；此前全部超时的根因未确认；不承诺 DNS 或节点超时已经修复。
