# 5.1.0-rc.2 验收记录

状态：**待实机验证，不具备稳定版发布结论。**

离线检查覆盖构建可复现、镜像一致、规则优先级、覆盖语法校验、未知域名代理、远程依赖消除及原有 DNS 参数。运行结果以具体提交的 CI 日志为准，不把测试数量当成网络质量指标。

| 手机侧检查 | 通过标准 | 当前结果 |
| --- | --- | --- |
| 版本记录 | 记录 iOS、Shadowrocket、配置 hash 与节点协议，不记录凭证 | 未测 |
| 导入与编译 | 两份独立配置无报错，可选中生效 | 未测 |
| 国内分流 | 常用支付、视频、聊天请求按预期 DIRECT | 未测 |
| 国外分流 | ChatGPT / Google / GitHub 的实际 HTTPS 请求成功且为 PROXY | 未测 |
| 广告误拦 | 对照 noads，必要功能可用，明确广告才拒绝 | 未测 |
| DNS / IPv4 / IPv6 | 两种网络检查 DNS 与公网出口，结合日志确定路径 | 未测 |
| WebRTC | 如使用网页通话，验证公网地址及通话可用性 | 未测 |
| 节点不可用 | 隧道保持开启时国外请求失败，不转为本地直连 | 未测 |
| 代理链与 UDP | 缺中转、不支持 UDP 的预期拒绝分别验证 | 未测 |
| 网络切换与唤醒 | Wi-Fi ↔ 蜂窝、锁屏唤醒后功能与出口符合预期 | 未测 |
| 性能对照 | 同节点同网络记录请求成功率、首包延迟与持续传输 | 未测 |
| 回退 | 可切回保存的已知可用配置并恢复原行为 | 未测 |

请将实测时间、样本数、成功/失败数写入发布记录。失败项先修复或明确限制适用范围；不填未经测试的延迟、速度、泄漏率或耗电成绩。
